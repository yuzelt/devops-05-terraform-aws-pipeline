apiVersion: v1
kind: Service
metadata:
  name: devops-05-terraform-aws-pipeline
  labels:
    app: devops-05-terraform-aws-pipeline
    team: frontend  
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: devops-05-terraform-aws-pipeline

